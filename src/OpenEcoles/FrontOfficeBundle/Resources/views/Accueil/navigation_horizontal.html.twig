<ul class="nav nav-pills">
    <li id="accueil" class="selected"><a href="{{path("open_ecoles_front_office_homepage")}}"><i class="icon-home"></i>Accueil</a></li>
    {% for menu in menuH %}
        <li id="{{ menu.nom }}" class=""><a href="{{path(menu.lien)}}">{{ menu.nom }}</a></li>
    {% else %}
        Il n'y a que l'accueil pas de bundle sur le serveur
    {% endfor %}
</ul>
