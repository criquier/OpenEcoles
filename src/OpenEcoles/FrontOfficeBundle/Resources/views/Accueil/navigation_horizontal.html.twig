<ul class="nav nav-pills">

    {% for bundle in bundles %}
        <li class=""><a href="{{path(bundle.accueil)}}">{{ bundle.menu_name }}</a></li>
    {% else %}
    Il n'y a que l'accueil pas de bundle sur le serveur
    {% endfor %}

</ul>
